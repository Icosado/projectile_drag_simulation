package simulations;

import physics.Environment;
import physics.Projectile;

public class Simulator {
    public static void main(String[] args) {
        Environment environment = Environment.EARTH_SEA;
        Projectile projectile = Projectile.CIRCLE;
    }
}
